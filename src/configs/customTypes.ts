export type Package = {
  id: number
  title: string
  slug: string
  image: string
  thumbnail_Image: string
  no_of_days: number
  summary: string
  full_detail: string
  is_active: boolean
  is_featured: boolean
  price: number
}
